name: example
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  wolt_modal_sheet:
    path: ..

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/
    - lib/assets/fonts/
