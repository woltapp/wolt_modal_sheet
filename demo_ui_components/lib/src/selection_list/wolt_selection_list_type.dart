/// Represents the selection type options for a WoltSelectionList widget.
enum WoltSelectionListType {
  /// Allows multiple items to be selected from a list.
  multiSelect,

  /// Allows only a single item to be selected at a time.
  singleSelect,
}
